    function generateRandomAlphanumeric(length) {
      return Math.random().toString(36).substring(2, length + 2);
    }
    let randomString = generateRandomAlphanumeric(10); // e.g., "g7h2j9k3"
    
    console.log(randomString)